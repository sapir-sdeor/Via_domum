using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

public class EventSystemDelete : MonoBehaviour
{
    private void Awake()
    {
        /*GameObject eventSystem = FindObjectOfType<EventSystem>().gameObject; 
        if (eventSystem && eventSystem != gameObject)
        {
            print("delete event system");
            Destroy(this);
        }*/
    }
}
